# tests/

Automated tests for the repository.

- `unit/` — configuration loader tests (`test_config_loader.py`).
- `integration/` — dataset discovery tests against a synthetic fixture
  (`test_dataset_discovery.py`).

> Tests require `pytest` (a dev dependency to be declared when packaging lands).
