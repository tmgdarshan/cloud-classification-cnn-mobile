# artifacts/

Local output location for generated artifacts (model checkpoints, metrics,
figures, logs).

**Contents are git-ignored** — artifacts are outputs of `code + config + data`,
not source. Only this README is tracked, to keep the directory present.
