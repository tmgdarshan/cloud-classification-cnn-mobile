# Training protocol: baseline.
#
# An early exploratory Adam / lr 1e-3 recipe. NOT researcher-approved and not
# used by the production runner (see config/training/tuned_resnet*.toml).
# Retained as a modular-config example.
name = "baseline"
approval_status = "draft"

optimizer = "adam"
learning_rate = 0.001
weight_decay = 0.001
batch_size = 16
num_epochs = 10
augmentation = "none"
seed = 42
